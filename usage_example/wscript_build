#!/usr/bin/env python

import os
PWD=os.getcwd()

usage_example = bld(
    features='cxx cprogram',
    source='main.cpp',
    target='logtest',
    includes='..',
    use = ['BOOST_THREAD', 'LOGGER', 'logger'],
    install_path=None,
)

usage_example = bld(
    features='cxx cprogram',
    source='main_c.c',
    target='main_c',
    includes='..',
    use=['BOOST_THREAD', 'LOGGER', 'logger_c'],
    install_path=None,
)
