#!/usr/bin/env python

import os

usage_example = bld(
    features='cxx cxxprogram',
    source='main.cpp',
    target='logtest',
    use = ['logger_obj'],
)

usage_example = bld(
    # ECM: c -> cxx fixes a problem with gold-linker on old lucid installs... *hack*
    features='cxx cxxprogram',
    source='main_c.c',
    target='main_c',
    use=['logger_c_obj', ],
)
