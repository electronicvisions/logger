#!/usr/bin/env python

import os
PWD=os.getcwd()

usage_example = bld(
    features='cxx cxxprogram',
    source='main.cpp',
    target='logtest',
    includes='..',
    linkflags='-lboost_thread',
    use = ['logger_obj'],
    install_path=None,
)

usage_example = bld(
    # ECM: c -> cxx fixes a problem with gold-linker on old lucid installs... *hack*
    features='cxx cxxprogram',
    source='main_c.c',
    target='main_c',
    includes='..',
    linkflags='-lboost_thread',
    use=['logger_c_obj', ],
    install_path=None,
)
